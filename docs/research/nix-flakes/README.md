# Nix Flakes Research

Nix Flakes is an experimental (but widely adopted) feature providing hermetic, reproducible package definitions with content-addressed lockfiles. Introduced in Nix 2.4 (2021) via RFC 0049, flakes are the de facto standard for Nix projects despite never being officially stabilized as of 2026.

## Documents

| File | Contents |
|------|----------|
| [internals.md](internals.md) | Lockfile format, `follows` resolver, eval cache, fetcher, `self` assembly |
| [source-code-deep-dive.md](source-code-deep-dive.md) | Annotated `call-flake.nix` source, lazy self-reference trap, `builtins.getFlake`, `nix-direnv` |
| [gotchas.md](gotchas.md) | Dirty trees, untracked files, `legacyPackages`, impure eval, `self` recursion |
| [advanced-topics.md](advanced-topics.md) | `buildGoModule`/`vendorHash`, `nix copy` SSH/NAR, `buildEnv`, CA derivations, `nixConfig` security, lazy trees, `nix flake metadata` |
| [ecosystem.md](ecosystem.md) | `flake-utils` vs `flake-parts`, binary caches, hermetic evaluation limits |
| [outputs-and-ci.md](outputs-and-ci.md) | Complete output schema, `nix flake check`, `nix develop` internals, GitHub Actions CI, `nix profile`, `nix-systems` |
| [deployment-and-secrets.md](deployment-and-secrets.md) | `nix copy` bootstrap flow, `--substitute-on-destination`, `nix store sign`, sops-nix/agenix, `cleanSourceWith`/`lib.fileset`, closure analysis (`path-info`, `why-depends`, `diff-closures`), channel strategy |
| [fods-ifd-and-internals.md](fods-ifd-and-internals.md) | Fixed-output derivations (network sandbox exception, hash modes, `impureEnvVars`), IFD semantics and perf, `nix shell`/`nix run` vs `nix develop`, narinfo wire format + signing, `callPackage`/`makeOverridable`, monorepo subflake patterns |
| [community.md](community.md) | RFC 136 stabilization plan, Lix/Determinate forks, design criticisms, `flake-compat`, alternatives |
| [tooling-and-patterns.md](tooling-and-patterns.md) | `nix eval` CI patterns, overlay fixpoint mechanics (`lib.fix`/`lib.extends`), `writeShellApplication` for `apps` output, `nixosModules` export patterns, remote builders, `nix repl` debugging, `buildNpmPackage` (two-phase FOD, Next.js sandbox problems, `importNpmLock`), `lib.fileset` API (`toSource`/`union`/`difference`/`gitTracked`/`fileFilter`/`maybeMissing`), `flake-parts` advanced patterns (`perSystem` args, `withSystem`, `readOnlyPkgs`) |
| [stdenv-and-packaging.md](stdenv-and-packaging.md) | All 9 stdenv build phases in order with env vars, `fixupPhase` deep dive (patchShebangs/patchELF/wrapProgram), multiple outputs (`$out`/`$dev`/`$lib`/`$man`), `installShellFiles`, cross-compilation (pkgsCross/three-platform model), `nixpkgs.config` in flakes (allowUnfree/legacyPackages gotcha), `pkgs.testers` (testVersion/testBuildFailure/runNixOSTest), `passthru`/`meta` conventions, `nix build --check` determinism, R13Y project |
| [docker-vms-scripts-fmt-security.md](docker-vms-scripts-fmt-security.md) | `pkgs.dockerTools` (buildImage/buildLayeredImage/streamLayeredImage, layer deduplication, contents vs config, fakeNss/shadowSetup, /etc/passwd problem, determinism, flake integration), NixOS VM tests as flake `checks` (Python driver full API, KVM requirements, interactive mode, specialisations, runtimes), `nix-shell` shebang scripts (-i/-p/--pure/--keep, flake shebang, nix-script tool), `treefmt`/`treefmt-nix` (`nix fmt` integration, flake-parts module, mtime caching vs Nix epoch problem, --fail-on-change), Nix store security (verify/repair/dump-path, sandbox properties, CVE-2024-38531, CVE-2024-51481, diff-hook abuse) |
| [drv-tooling-mkshell-modules.md](drv-tooling-mkshell-modules.md) | `.drv` ATerm wire format, `nix-instantiate` (eval/parse/strict modes, `--eval` discards drvs), `nix derivation show` JSON schema, `nix-store --query` graph traversal (`--references` vs `--referrers` vs `--requisites`), `nix-store --realise` vs `nix build`, `nix store` subcommands (ls/cat/diff-closures/make-content-addressed/sign/repair/gc/info); `pkgs.mkShell` source walkthrough (`packages`→`nativeBuildInputs`, `inputsFrom` mergeInputs, `shellHook` concat order), `mkShellNoCC` (`stdenvNoCC` omits gcc/binutils), CC/CXX/NIX_CFLAGS_COMPILE injection, `packages` vs `buildInputs` distinction; `lib.evalModules` entry point, `lib.types` merge rules (str errors on conflict, listOf concatenates, submodule recurses), `mkIf`/`mkMerge`/`mkOverride` priority system (1500/1000/100/50/10), `mkRenamedOptionModule`/`mkRemovedOptionModule`/`mkAliasOptionModule`, `config` vs `options` namespace, common module system mistakes |
| [stdlib-and-builtins.md](stdlib-and-builtins.md) | `nixpkgs.lib` deep dive (`lib.strings`: `escapeNixIdentifier`, `replaceStrings` no-regex gotcha, `splitString`, `concatMapStringsSep`, `toUpper`/`toLower` ASCII-only, `sanitizeDerivationName`, `nameValuePair`, `fixedWidthNumber`; `lib.attrsets`: `recursiveUpdate` semantics, `mapAttrs'`, `filterAttrs`, `genAttrs`, `attrByPath`/`getAttrFromPath`/`setAttrByPath`, `foldlAttrs` nixpkgs-23.05, `zipAttrsWith`; `lib.lists`: `foldl'` strict, `imap0`/`imap1`, `flatten` vs `concatLists`, `subtractLists`, `unique`, `naturalSort`, `range`, `toposort`; `lib.trivial`: `pipe`, `flip`, `const`, `warn`/`warnIf`/`traceIf`, `importJSON`/`importTOML`, `defaultTo`; `lib.filesystem` and `lib.path` modules); hermetic eval builtins (`currentSystem`/`currentTime` absent-not-zero in pure mode, `builtins.getEnv` silent empty string, `storePath` reproducibility hazard, `builtins.path` FOD mode, `--impure` scope, `restrict-eval`/`allowed-uris` prefix matching, `__impure` derivations, `accept-flake-config` security); `nix flake` subcommands (`init`/`new` template system with `path`/`description`/`welcomeText`, `update` selective inputs and `--update-input` deprecation, `clone`, `show --json` vs `check`, `prefetch` NAR hash, `nix registry` types/priority/Nix-2.26-scope-change/`pin`) |
| [gc-bundlers-eval-performance.md](gc-bundlers-eval-performance.md) | GC root universe (filesystem/profile/temproot/runtime `/proc` scan), mark/sweep algorithm, `--max-freed`, `nix-store --add-root --indirect` mechanics, `keep-outputs`/`keep-derivations` nix.conf, `nix-store --optimise` hardlink dedup via `.links/`, `auto-optimise-store` latency gotcha, reference scanning (`RefScanSink` streaming base32 grep), `allowedReferences`/`disallowedReferences`/`allowedRequisites`; `bundlers.<system>.<name>` flake output schema, `nix bundle` CLI, `matthewbauer/nix-bundle` (arx/self-extracting), `ralismark/nix-appimage` (SquashFS+FUSE), `github:NixOS/bundlers` inventory (`toArx`/`toAppImage`/`toDockerImage`/`toRPM`/`toDEB`), `pkgs.appimageTools` distinction; thunk lifecycle (`tThunk`/`tBlackHole`/in-place memoization), `seq` WHNF vs `deepSeq` full force, `lib.foldl'` WHNF-only gotcha for attrset accumulators, string vs path coercion (path interpolation copies to store at eval time, `toString` does not), `outPath` coercion and string context trap, `builtins.toJSON` as deep-force workaround, `NIX_SHOW_STATS`/`NIX_COUNT_CALLS` counters, `--eval-profiler flamegraph` (Nix ≥ 2.30), `nix-eval-jobs` parallel workers + `--gc-roots-dir`, `nixpkgs.lib` vs `pkgs.lib` import cost, `legacyPackages` memoization vs `import nixpkgs {}`, `flake-parts` perSystem laziness, `mapAttrs` lazy-per-key vs `attrValues` forces all, `python3Packages` fixpoint trap |
| [home-manager-darwin-compat.md](home-manager-darwin-compat.md) | Home Manager as flake consumer: `homeConfigurations.<name>` output, `homeManagerConfiguration` (`pkgs`/`modules`/`extraSpecialArgs`), `inputs.nixpkgs.follows` binary-cache trick, `home-manager switch --flake .#user@host` activation mechanics (GC roots, collision detection, generations), `home.stateVersion` footgun, `programs.<name>` vs `home.packages` distinction, `home.file` symlink mechanics, `home.activation` DAG (`entryAfter`/`entryBefore`/`writeBoundary`), `home.sessionVariables` new-shell-only caveat, NixOS+HM combined flake (`home-manager.nixosModules.home-manager`), `useGlobalPkgs`/`useUserPackages`, standalone vs NixOS module mode comparison; **nix-darwin** (`darwinConfigurations.<hostname>`, `darwin.lib.darwinSystem` vs `nixosSystem` differences, `homebrew.*` options, `onActivation.cleanup` zap-vs-uninstall gotcha, brew prefix per-arch, `system.defaults.*` macOS preferences, `launchd.agents`/`launchd.daemons`, what nix-darwin cannot manage, `/run` synthetic symlink Catalina fix, Apple Silicon vs Intel `nixpkgs.hostPlatform`); **flake-compat** (`NixOS/flake-compat` vs `edolstra` origin, bridge pattern `default.nix`/`shell.nix`, `defaultNix`/`shellNix`/`outputs` return values, `self.rev` unavailable, lock file version v4–v7, `follows` resolution, inputs fetched via tarball cache, `flake = false` convention, `builtins.getFlake` distinction) |
| [language-packaging-and-nixpkgs-contrib.md](language-packaging-and-nixpkgs-contrib.md) | Language-specific packaging deep dives: **Python** (`buildPythonPackage` vs `buildPythonApplication` propagation semantics, `pythonPackages.callPackage` scope, `propagatedBuildInputs` runtime closure, `format` attribute values, `pythonImportsCheck`, `pythonRelaxDepsHook`/`pythonRemoveTestsDir`); **Rust** (`cargoHash` FOD vs `cargoLock` direct use, `outputHashes` for git deps, `allowBuiltinFetchGit`, `buildFeatures`/`buildType`/`checkFlags`, cross-compilation via `pkgsCross`); **JavaScript** (`npmHooks` system: `npmConfigHook`/`npmBuildHook`/`npmInstallHook`, `importNpmLock` no-hash approach, `mkYarnPackage`/`yarn2nix-moretea` offline workflow, `node2nix` code-generation approach and deprecation, `fetchPnpmDeps`/`pnpmConfigHook` pnpm support); **Go** (`CGO_ENABLED` default=1, `subPackages`, `vendorHash` NAR semantics, `proxyVendor`, `ldflags`/`tags`/`allowGoReference`, `gomod2nix` eliminating `vendorHash` churn); **Nixpkgs contribution workflow** (branch structure: `master`/`staging-next`/`staging`/release, channel advancement: `nixos-unstable-small` vs `nixos-unstable` vs `nixpkgs-unstable` Hydra jobset differences); **ofborg** history and 2024–2025 retirement, GitHub Actions replacement, PR rebuild labels; **`nixpkgs-review`** (step-by-step PR workflow, ofborg/GA eval integration, `--post-result`, `--no-shell` scripting); **`nix-update`** (`--version=branch`, `--flake`, hash auto-computation, `nix-update-script` in `passthru.updateScript`, `r-ryantm` bot); **`pkgs/by-name`** (RFC 140, two-letter shard structure, `nixpkgs-vet` CI enforcement, migration status ~97%, `finalAttrs` pattern) |
| [hydra-and-nixos-patterns.md](hydra-and-nixos-patterns.md) | **Hydra self-hosted CI**: `hydraJobs` as the only Hydra-native output type, `lib.recurseIntoAttrs` (`recurseForDerivations` marker, per-attrset scope, silent skip hazard), flake jobset configuration (`builtins.getFlake`, restricted mode, `nix.settings.allowed-uris`), hydra-build-products file format (`file`/`doc`/`report` types), `hydra-channel-name`, aggregate jobs (`pkgs.releaseTools.aggregate`, string constituents for GC optimization), `nix-eval-jobs` (JSON Lines schema, `--gc-roots-dir` race prevention, `--workers`/`--max-memory-size`, NixOS/nix-eval-jobs vs nix-community fork), `services.hydra` NixOS module (all options table, PostgreSQL dependency, `extraConfig` for `store_uri` S3 upload + `binary_cache_secret_key_file`), Hydra as binary cache substituter; **NixOS patterns**: `system.activationScripts` DAG (`deps` list, `textClosureList` topo-sort, `supportsDryActivation`), specialisations (`inheritParentConfig`, bootloader entries per-generation, runtime switch via `switch-to-configuration`/`nixos-rebuild --specialisation`, `/run/current-system/specialisation/<name>/`), `systemd.services` hardening (`DynamicUser` ephemeral UID, `StateDirectory`/`CacheDirectory`/`LogsDirectory`/`RuntimeDirectory` auto-creation, `PrivateTmp`, `ProtectSystem`/`ProtectHome` value semantics, `BindPaths`/`BindReadOnlyPaths`, `CapabilityBoundingSet`, `SystemCallFilter` predefined groups), `systemd.tmpfiles.rules` (`d`/`D`/`f`/`L`/`L+`/`z`/`Z`/`r`/`R` types, age-based cleanup, `systemd.tmpfiles.settings` structured form), `networking.nftables` (atomic rule replacement vs iptables, `nixos-fw` table auto-generation, `networking.nftables.tables` custom chains), `virtualisation.oci-containers` (podman default since 22.05, full option table, `dependsOn` systemd integration, `imageFile` from Nix store, `sdnotify` modes), `nixpkgs.overlays` in modules (system pkgs scope vs flake-level export, `nixpkgs.config.allowUnfree` scope limitation), `hardware.enableRedistributableFirmware` (firmware packages list, `enableAllFirmware` superset, server relevance), impermanence pattern (`environment.persistence`, bind mount mechanism, btrfs subvolume approach, `/etc/machine-id` gotcha), `system.etc.overlay.enable` (EROFS lower layer, overlayfs, mutable/immutable modes, `nixos-install` incompatibility) |
| [lang-advanced-determinate-fleet.md](lang-advanced-determinate-fleet.md) | **Nix language advanced**: `with` scoping rules (explicit vs implicit definitions, let-wins-over-with, nixpkgs ban rationale), `inherit (x)` vs `inherit` error modes, `rec {}` shadowing bug vs `let..in` safety, string context three-element-type model (`builtins.getContext`/`appendContext`/`hasContext`/`unsafeDiscardStringContext`), `builtins.derivation` raw interface (mandatory/optional attrs, `__structuredAttrs` JSON env file, `passAsFile`, `outputChecks`), `builtins.placeholder`, `assert` statement vs NixOS `assertions` option, `builtins.tryEval` (catches `throw`/`assert` not `abort`/type-errors), `__functor` callable attrsets (`makeExtensible`, `makeOverridable` pattern), `builtins.addErrorContext`; **Determinate Nix vs Lix vs CppNix**: three-distribution comparison table, Determinate features (stable flakes, lazy trees default 3.8.0, parallel eval, FlakeHub native integration, `fh` CLI, FlakeHub Cache, `receipt.json` installer), CppNix governance (RFC 49 anomaly, 2024 crisis, Meson migration Nix 2.26), Lix (community fork of CppNix 2.18, `lix-module` NixOS installation, correctness-focused, no lazy trees/FlakeHub); **Fleet management**: `nixosConfigurations` multi-host with `specialArgs`, `deploy-rs` flake schema (`deploy.nodes.<name>.profiles.<name>`, `activate.nixos`/`activate.custom`, magic rollback, CLI commands), `colmena` (hive.nix structure, `meta`/`defaults`/per-host, `deployment.*` options, `colmena apply --goal switch/boot/test/dry-activate`, `--on @tag` glob targeting, `deployment.keys` out-of-band secrets), `nixos-anywhere` (kexec bootstrap, `--flake .#host` syntax, disko integration, prerequisites), `disko` (device type hierarchy, ZFS `zpool`+`datasets` example, `--mode format/mount/destroy`, `disko.nixosModules.disko`), `morph` legacy tool, tool comparison table (`nix copy` vs deploy-rs vs colmena vs morph) |
| [binary-caches-and-fetchers.md](binary-caches-and-fetchers.md) | **Self-hosted binary caches**: `nix-cache-info` protocol fundamentals, signing key generation/rotation (`nix-store --generate-binary-cache-key`, Ed25519 format, `nix store sign`), **nix-serve** (Perl, `NIX_SECRET_KEY_FILE`, `services.nix-serve.*` NixOS module, limitations), **nix-serve-ng** (Haskell/Warp, libnixstore FFI, 31× faster NAR throughput, drop-in via `package = pkgs.nix-serve-ng`), **harmonia** (Rust, async, zstd compression, HTTP range requests, Prometheus `/metrics`, `services.harmonia.*` with `signKeyPaths`/`settings`), **attic** (multi-tenant, FastCDC chunked NAR storage, two-level deduplication, PostgreSQL+S3 backend, `attic push/use/login`, `atticadm make-token`, early-prototype status), **cachix** (SaaS, `cachix push/use/authtoken`, GitHub Actions `cachix-action`, free tier limits, not self-hostable); **post-build-hook**: `nix.conf` `post-build-hook=` setting, `$OUT_PATHS`/`$DRV_PATH` env vars, root execution context, blocking gotcha + async queue pattern; **S3 binary cache**: full URL parameter table (`region`/`endpoint`/`profile`/`compression`/`secret-key`/`parallel-compression`), Cloudflare R2 and Backblaze B2 endpoint examples, `file://` local cache; **Flake input fetcher types**: `github:`/`gitlab:`/`sourcehut:` (archive API, no submodules, `host=` for enterprise), `git+https:`/`git+ssh:` (full clone, `shallow=1`/`allRefs`/`submodules=1` parameters), `git+file:` (local git, object semantics), `path:` (filesystem, narHash lock), `tarball:`/`file:` (HTTP, narHash only), `hg+` (Mercurial), `dir=` universal query parameter; `flake = false` attribute; `follows` deduplication; fetcher quick-reference table |
| [store-paths-nixpath-fetchgit.md](store-paths-nixpath-fetchgit.md) | **Store path hash computation** (universal fingerprint formula, Nix base-32 encoding, 160-bit truncation; source paths: NAR hash → `source:sha256:…:/nix/store:name`; text paths for `.drv`/`builtins.toFile`; input-addressed output paths: modulo-fixed-output ATerm hash → `output:out:sha256:…`; fixed-output paths: two-stage `fixed:out:rec:algo:hash:` → `output:out:`; CA derivations early-cutoff; `builtins.hashString`/`hashFile` algorithms; `lib.fakeHash`/`lib.fakeSha256` trick; `--check` non-determinism detection; pointer equality guarantee); **NIX_PATH and angle brackets** (`<nixpkgs>` desugars to `builtins.findFile builtins.nixPath`; colon-separated `name=path` format; NIX_PATH empty in pure flake eval; `nix.nixPath` NixOS option; how channels set NIX_PATH; `nix-channel` subcommands and channel profile mechanics; `~/.nix-channels` format; nixos-unstable vs nixpkgs-unstable difference; `nix.registry` vs `nix.nixPath` comparison; `nix-env` ↔ `nix profile` command mapping table; `manifest.nix` vs `manifest.json` v1 incompatibility; `nix-env --query`/`--upgrade` flags); **`builtins.fetchGit`** (all parameters with types/defaults table; `rev` vs `ref` hermetic vs impure; `allRefs = true` for non-default-branch revs and PR heads; `submodules = true` cost and MountedSourceAccessor mechanics; `shallow = true` tradeoffs; git cache at `~/.cache/nix/gitv3/<sha256>/`; dirty working tree: `dirtyRev`/`dirtyShortRev` attributes; `lastModified` Unix timestamp; `builtins.fetchTree` comparison; `flakes` feature gate on `fetchTree`; `builtins.path` vs `fetchGit ./` for local sources) |
| [nixos-rebuild-and-version-history.md](nixos-rebuild-and-version-history.md) | **`nixos-rebuild` deep mechanics**: all modes compared (switch/boot/test/dry-activate/build/build-vm) with activation matrix, `switch-to-configuration` step-by-step (symlink, activationScripts DAG, systemd daemon-reload, unit diffing, bootloader), systemd unit restart vs reload vs nothing (`restartIfChanged`/`reloadIfChanged`/`stopIfChanged`/`X-Restart-Triggers`/`X-Reload-Triggers`), profile generations (`/nix/var/nix/profiles/system-N-link`, GC roots, rollback), specialisations runtime switching; **remote deployment patterns**: `--target-host` flow (build local + `nix copy ssh-ng://` + SSH activation), `--build-host`, `--use-remote-sudo` env-var stripping gotcha, SSH ControlMaster reuse and the ControlMaster+`nix copy` deadlock workaround, `NIXOS_INSTALL_BOOTLOADER=1`, zero-downtime `test`-then-`switch` pattern, kernel upgrade `boot`+`reboot` pattern, dead man's switch auto-rollback, `trusted-users` requirement; **`nixos-rebuild-ng`** Python rewrite (NixOS 25.05 opt-in, 25.11 default); **Nix version history 2.4–2.30**: per-release feature table covering what breaks existing flakes, new CLI commands, and flake workflow capabilities (output naming, `--update-input` removal, `nix repl` syntax change, `nix hash` rename, `nix-shell` dir behavior, lock file format changes, eval profiler, value size optimization) |
| [generators-trivial-builders-images.md](generators-trivial-builders-images.md) | **`lib.generators`**: `mkKeyValueDefault`/`mkValueStringDefault` primitives, `toKeyValue` (env files, `listsAsDuplicateKeys`), `toINI`/`toINIWithGlobalSection`/`toGitINI`, `toPretty` (function/derivation/float rendering), `withRecursion` depth limit, `toYAML`/`toDHALL`/`toLua`/`toPlist`; **`pkgs.formats`**: return-value shape (`type`/`generate`/`lib`), all core formats (`json`/`yaml`/`yaml_1_2`/`toml`/`ini`/`iniWithGlobalSection`/`keyValue`/`gitIni`), extended formats (`elixirConf`/`lua`/`nixConf`/`pythonVars`/`xml`/`plist`/`hcl1`), the **`settingsFormat` pattern** for NixOS module authoring (`freeformType` + `format.type` + `format.generate`); **trivial builders**: `runCommandWith` primitive, `runCommand`/`runCommandLocal`/`runCommandCC` distinctions (stdenvNoCC vs stdenv, `preferLocalBuild`), `writeText`/`writeTextFile` (full options: `executable`, `destination`, `checkPhase`, `$target`), `writeTextDir`/`writeScript`/`writeShellScript`/`writeShellScriptBin`, `linkFarm` (list vs attrset entries, `passthru.entries`), `symlinkJoin` (lndir implementation, `stripPrefix`, `buildEnv` comparison); **disk image generation**: `system.build` namespace (`toplevel`/`isoImage`/`sdImage`/`image`), `isoImage.*` options, `make-disk-image.nix` parameters, `nixos-rebuild build-image` (NixOS 25.05+ upstreaming of nixos-generators, all variants, `image.modules` per-variant customization), `system.build.image` via repart module, kexec for `nixos-anywhere`; **`nix flake check` deep dive**: per-output behavior matrix (what is evaluated vs built), `checks` vs `packages` distinction, `nixosConfigurations` evaluation depth (assertions checked, build not triggered), `apps` schema validation, `--no-build` evaluation-only mode (IFD still triggers), `--keep-going`/`--print-build-logs` for CI; **factual fix**: `symlinkJoin` uses `lndir` directly, not `buildEnv` wrapper |
| [search-fetchclosure-python-fmt.md](search-fetchclosure-python-fmt.md) | **`nix search` internals**: `legacyPackages` + `recurseForDerivations` traversal, eval-cache-v6 behavior (cold 30–90s, invalidated on narHash change), `--json` output schema `{ attrPath: { pname, version, description } }`, `--exclude` regex, Nix 2.20 requires explicit `^` for "all packages"; **`nix-index`/`nix-locate`**: NAR-index-based file search, `--whole-name`/`--type`/`--regex` flags, database at `~/.cache/nix-index/files`, `command-not-found` shell integration; **`nix-index-database`**: weekly pre-built flake (`nixosModules.default`/`homeModules.default`/`darwinModules.nix-index`), `programs.nix-index-database.comma.enable`; **`comma`**: nix-locate + `nix shell` runner, three caching levels (0/1/2), cache at `~/.cache/comma/`; **`nix-search-cli`**: queries search.nixos.org ElasticSearch API, no local eval; **`builtins.fetchClosure`**: `fetch-closure` experimental feature (Nix ≥ 2.8), three patterns (CA trustless / IA→CA rewrite via `toPath` / IA direct requiring `trusted-public-keys`), `nix store make-content-addressed` to find `toPath`, comparison to `builtins.storePath` (storePath requires path to exist locally; fetchClosure downloads), security model, use cases; **`python3.withPackages`**: wraps `python.buildEnv`, `pythonModule` passthru filter, `.env` attribute, `buildEnv.override` for `ignoreCollisions`/`postBuild`, `python3.pkgs` ≡ `python3Packages` alias, devShell patterns (withPackages vs direct python3Packages), multiple version matrix, `python3.pkgs.callPackage` via `packageOverrides`; **`poetry2nix`**: unmaintained as of 2025, `mkPoetryApplication`/`mkPoetryEnv`/`mkPoetryPackages`, `defaultPoetryOverrides.extend`, groups support; **`pyproject.nix`**: foundation library (PEP-621/440/508 parsing), used by uv2nix; **`uv2nix`**: active replacement for poetry2nix, `workspace.loadWorkspace`, `mkPyprojectOverlay`, `sourcePreference` wheel vs sdist, `python-set.mkVirtualEnv`, `workspace.deps.default`; **Formatter ecosystem**: `nixpkgs-fmt` (archived/deprecated), `alejandra` v4.0.0 (opinionated Rust, widely used, `formatter.<system> = pkgs.alejandra`), `nixfmt-rfc-style` (official RFC 166, Haskell, nixpkgs migrating in 2025–2026, `nixfmt-tree --check`), treefmt integration for both, pre-commit hook patterns |

## Key Facts for forge-metal

The `flake.nix` in this repo uses:
- `nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable"` — single nixpkgs instance, avoids diamond dependency binary cache misses
- `flake-utils.lib.eachDefaultSystem` — iterates across `["x86_64-linux" "aarch64-linux" "x86_64-darwin" "aarch64-darwin"]`
- `self.packages.${system}.default` inside `serverProfile` — references the Go binary derivation from within the same flake; works because `self` is lazily evaluated
- `self.shortRev or "dev"` — `self.shortRev` throws in dirty worktrees; the `or` catches the eval error
- `buildGoModule` with `vendorHash` for `cmd/bmci` — hash covers the NAR of `vendor/`; update with `lib.fakeHash` trick after `go.mod` changes
- `nix copy --to ssh://host` uses `ServeProto` (legacy); `ssh-ng://` uses full daemon protocol; compression off by default

The eval cache is **disabled** for local path flakes. `nix develop .` never hits the eval cache — every shell invocation re-evaluates `flake.nix`. Use `nix-direnv` to cache devShell builds.

## Why Flakes

| Without Flakes | With Flakes |
|----------------|-------------|
| `nix-channel` per-user state; channels diverge between machines | `flake.lock` pins all inputs; same commit = same build |
| `NIX_PATH` implicit global; easy to contaminate | Pure eval by default; network/env access blocked |
| No first-class multi-output structure | Typed schema: `packages`, `devShells`, `nixosConfigurations`, etc. |
| `default.nix` fetches at eval time | All fetching at lock time; eval is hermetic |

## Historical Context

RFC 0049 was authored by Eelco Dolstra but was **withdrawn** after the implementation was already merged — a procedural anomaly that raised governance questions. Flakes remain behind `--extra-experimental-features nix-command flakes` in the official Nix installer defaults. RFC 136 (accepted August 2023) established a phased stabilization plan; see [community.md](community.md) for current phase status.
