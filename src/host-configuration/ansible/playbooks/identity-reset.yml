---
# Wipe-only: aspect host-configuration reset identity invokes this playbook first,
# then runs the site playbook to rebuild per-component state.

- name: Wipe identity-service PostgreSQL state
  import_playbook: wipe-pg-db.yml
  vars:
    wipe_pg_db_name: identity_service
