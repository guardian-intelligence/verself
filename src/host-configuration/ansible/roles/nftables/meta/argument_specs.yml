---
argument_specs:
  main:
    short_description: "Host firewall via nftables (default-deny ingress)"
    description:
      - "Copies authored host nftables files."
      - "The topology ruleset records are the topology contract; Ansible only installs them."
    options: {}
