load("@io_bazel_rules_go//go:def.bzl", "go_library")

# Generated by `cue exp gengotypes ./...` from src/cue-renderer/.
# Refresh after editing versions.cue and check the result in. No
# consumers yet — wired up when the catalog.yml renderer lands.
go_library(
    name = "catalog",
    srcs = ["cue_types_gen.go"],
    importpath = "github.com/verself/cue-renderer/catalog",
    visibility = ["//visibility:public"],
)
