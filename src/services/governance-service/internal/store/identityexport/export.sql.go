// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: export.sql

package identityexport

import (
	"context"
)

const exportIdentityAPICredentialsJSONL = `-- name: ExportIdentityAPICredentialsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT c.credential_id,
           c.org_id,
           c.subject_id,
           c.client_id,
	           c.display_name,
	           c.auth_method,
	           c.status,
	           c.created_at,
	           c.created_by,
	           c.updated_at,
	           c.expires_at,
	           c.revoked_at,
	           c.revoked_by,
	           c.last_used_at
	    FROM iam_api_credentials c
	    WHERE c.org_id = $1
	    ORDER BY c.created_at,
	             c.credential_id
) t
`

type ExportIdentityAPICredentialsJSONLParams struct {
	OrgID string
}

func (q *Queries) ExportIdentityAPICredentialsJSONL(ctx context.Context, arg ExportIdentityAPICredentialsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportIdentityAPICredentialsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
