haproxy_public_routes:
  - host: '{{ notifications_service_domain }}'
    backend: be_route_product_notifications_api_notifications_service_public_api
haproxy_public_apis:
  - key: notifications
    host: '{{ notifications_service_domain }}'
    path_prefix: /api/v1
