haproxy_public_routes:
  - host: '{{ secrets_service_domain }}'
    backend: be_route_product_secrets_api_secrets_service_public_api
haproxy_public_apis:
  - key: secrets
    host: '{{ secrets_service_domain }}'
    path_prefix: /api/v1
