haproxy_public_routes:
  - host: '{{ projects_service_domain }}'
    backend: be_route_product_projects_api_projects_service_public_api
haproxy_public_apis:
  - key: projects
    host: '{{ projects_service_domain }}'
    path_prefix: /api/v1
