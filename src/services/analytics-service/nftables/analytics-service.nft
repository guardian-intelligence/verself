# Owner-local nftables file.
# Owner-local nftables ruleset
# nftables ruleset analytics_service.
table inet verself_analytics_service
delete table inet verself_analytics_service

table inet verself_analytics_service {
    chain input {
        type filter hook input priority filter; policy accept;

        tcp dport 20000-32000 iifname != "lo" drop
    }
}
