load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "reconcile-cloudflare-dns_lib",
    srcs = [
        "cloudflare.go",
        "main.go",
    ],
    importpath = "github.com/verself/substrate/cmd/reconcile-cloudflare-dns",
    visibility = ["//visibility:private"],
    deps = ["@in_gopkg_yaml_v3//:yaml_v3"],
)

go_binary(
    name = "reconcile-cloudflare-dns",
    embed = [":reconcile-cloudflare-dns_lib"],
    visibility = ["//visibility:public"],
)
