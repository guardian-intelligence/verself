---
# Exhaustively wipe identity-service PostgreSQL state, then run the canonical
# deploy path.

- name: Wipe identity-service PostgreSQL state
  import_playbook: wipe-pg-db.yml
  vars:
    wipe_pg_db_name: identity_service

- name: Re-apply substrate after identity reset
  import_playbook: box.yml
