---
# Exhaustively wipe billing ledger state on an existing environment, then run
# the canonical deploy path.

- name: Wipe billing PostgreSQL and ledger state
  import_playbook: wipe-pg-db.yml
  vars:
    wipe_pg_db_name: billing
