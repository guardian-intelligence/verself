[defaults]
# Inventory is supplied by `aspect render --site=<site>` and threaded
# through ansible-with-tunnel.sh as -i .cache/render/<site>/inventory/.
# Setting a default here would mask configuration errors.
roles_path = roles
library = plugins/modules
action_plugins = plugins/action
callbacks_enabled = ansible.posix.profile_tasks, community.general.opentelemetry
host_key_checking = False
timeout = 30
fact_caching = jsonfile
fact_caching_connection = .cache/facts
fact_caching_timeout = 86400
vars_plugins_enabled = host_group_vars,community.sops.sops

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=10m -o ControlPath=~/.ssh/sockets/%r@%h-%p -o IPQoS=none -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -o ConnectTimeout=10
pipelining = True
