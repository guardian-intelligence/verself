# Substrate

Substrate owns host and daemon convergence inputs. The current digest target
intentionally points at the existing platform tree while the package cutover is
in progress; deploy orchestration consumes only the digest target.
