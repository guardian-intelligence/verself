# Empty package marker for the github.com/verself/attestation Go module.
# Go package targets live under predicate/, bundle/, transit/, and conformance/.

load("//src/tools/build:go_module_checks.bzl", "go_module_checks")

go_module_checks()
