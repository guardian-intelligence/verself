// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: export.sql

package sandboxexport

import (
	"context"
)

const exportSandboxExecutionAttemptsJSONL = `-- name: ExportSandboxExecutionAttemptsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT a.attempt_id, a.execution_id, a.attempt_seq, a.state, a.lease_id, a.exec_id, a.billing_job_id, a.failure_reason, a.exit_code, a.duration_ms, a.zfs_written, a.stdout_bytes, a.stderr_bytes, a.rootfs_provisioned_bytes, a.boot_time_us, a.block_read_bytes, a.block_write_bytes, a.net_rx_bytes, a.net_tx_bytes, a.vcpu_exit_count, a.trace_id, a.started_at, a.completed_at, a.created_at, a.updated_at
    FROM execution_attempts a
    JOIN executions e ON e.execution_id = a.execution_id
    WHERE e.org_id::text = $1
    ORDER BY a.created_at,
             a.attempt_id
) t
`

type ExportSandboxExecutionAttemptsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionAttemptsJSONL(ctx context.Context, arg ExportSandboxExecutionAttemptsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionAttemptsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionBillingWindowsJSONL = `-- name: ExportSandboxExecutionBillingWindowsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT bw.attempt_id, bw.billing_window_id, bw.window_seq, bw.reservation_shape, bw.reserved_quantity, bw.actual_quantity, bw.reserved_charge_units, bw.billed_charge_units, bw.writeoff_charge_units, bw.cost_per_unit, bw.pricing_phase, bw.state, bw.window_start, bw.created_at, bw.settled_at, bw.reservation_jsonb
    FROM execution_billing_windows bw
    JOIN execution_attempts a ON a.attempt_id = bw.attempt_id
    JOIN executions e ON e.execution_id = a.execution_id
    WHERE e.org_id::text = $1
    ORDER BY bw.window_start,
             bw.billing_window_id
) t
`

type ExportSandboxExecutionBillingWindowsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionBillingWindowsJSONL(ctx context.Context, arg ExportSandboxExecutionBillingWindowsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionBillingWindowsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionEventsJSONL = `-- name: ExportSandboxExecutionEventsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT ev.event_seq, ev.execution_id, ev.attempt_id, ev.from_state, ev.to_state, ev.reason, ev.trace_id, ev.created_at
    FROM execution_events ev
    JOIN executions e ON e.execution_id = ev.execution_id
    WHERE e.org_id::text = $1
    ORDER BY ev.created_at,
             ev.event_seq
) t
`

type ExportSandboxExecutionEventsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionEventsJSONL(ctx context.Context, arg ExportSandboxExecutionEventsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionEventsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionFilesystemMountsJSONL = `-- name: ExportSandboxExecutionFilesystemMountsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT m.execution_id, m.mount_name, m.source_ref, m.mount_path, m.fs_type, m.read_only, m.sort_order, m.created_at
    FROM execution_filesystem_mounts m
    JOIN executions e ON e.execution_id = m.execution_id
    WHERE e.org_id::text = $1
    ORDER BY m.execution_id,
             m.sort_order,
             m.mount_name
) t
`

type ExportSandboxExecutionFilesystemMountsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionFilesystemMountsJSONL(ctx context.Context, arg ExportSandboxExecutionFilesystemMountsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionFilesystemMountsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionLogsJSONL = `-- name: ExportSandboxExecutionLogsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT l.execution_id, l.org_id, l.attempt_id, l.seq, l.stream, l.chunk, l.created_at
    FROM execution_logs l
    JOIN executions e ON e.execution_id = l.execution_id
    WHERE e.org_id::text = $1
    ORDER BY l.attempt_id,
             l.seq
) t
`

type ExportSandboxExecutionLogsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionLogsJSONL(ctx context.Context, arg ExportSandboxExecutionLogsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionLogsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionStickyDiskMountsJSONL = `-- name: ExportSandboxExecutionStickyDiskMountsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT m.mount_id, m.execution_id, m.attempt_id, m.allocation_id, m.mount_name, m.key_hash, m.key, m.mount_path, m.base_generation, m.source_ref, m.target_source_ref, m.save_requested, m.save_state, m.committed_generation, m.committed_snapshot, m.failure_reason, m.sort_order, m.requested_at, m.started_at, m.completed_at, m.created_at, m.updated_at
    FROM execution_sticky_disk_mounts m
    JOIN executions e ON e.execution_id = m.execution_id
    WHERE e.org_id::text = $1
    ORDER BY m.created_at,
             m.mount_id
) t
`

type ExportSandboxExecutionStickyDiskMountsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionStickyDiskMountsJSONL(ctx context.Context, arg ExportSandboxExecutionStickyDiskMountsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionStickyDiskMountsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxExecutionsJSONL = `-- name: ExportSandboxExecutionsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (SELECT execution_id, org_id, actor_id, kind, source_kind, workload_kind, source_ref, runner_class, external_provider, external_task_id, provider, product_id, state, correlation_id, idempotency_key, run_command, max_wall_seconds, requested_vcpus, requested_memory_mib, requested_root_disk_gib, requested_kernel_image, created_at, updated_at FROM executions WHERE org_id::text = $1 ORDER BY created_at, execution_id) t
`

type ExportSandboxExecutionsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxExecutionsJSONL(ctx context.Context, arg ExportSandboxExecutionsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxExecutionsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxGithubInstallationsJSONL = `-- name: ExportSandboxGithubInstallationsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT
        c.connection_id,
        c.org_id,
        c.connected_by_actor_id,
        c.state AS connection_state,
        c.created_at AS connected_at,
        c.updated_at AS connection_updated_at,
        i.installation_id,
        i.account_id,
        i.active AS installation_active,
        i.repository_selection,
        i.permissions_json,
        i.created_at AS installation_created_at,
        i.updated_at AS installation_updated_at,
        a.account_login,
        a.account_type,
        a.created_at AS account_created_at,
        a.updated_at AS account_updated_at
    FROM github_installation_connections c
    JOIN github_installations i ON i.installation_id = c.installation_id
    JOIN github_accounts a ON a.account_id = i.account_id
    WHERE c.org_id::text = $1
    ORDER BY c.created_at, i.installation_id
) t
`

type ExportSandboxGithubInstallationsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxGithubInstallationsJSONL(ctx context.Context, arg ExportSandboxGithubInstallationsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxGithubInstallationsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxRunnerAllocationsJSONL = `-- name: ExportSandboxRunnerAllocationsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT a.allocation_id, a.provider, a.provider_installation_id, a.provider_repository_id, a.runner_class, a.runner_name, a.provider_runner_id, a.execution_id, a.attempt_id, a.state, a.requested_for_provider_job_id, a.allocate_by, a.jit_by, a.vm_submitted_by, a.runner_listening_by, a.assignment_by, a.vm_exit_by, a.cleanup_by, a.failure_reason, a.created_at, a.updated_at
    FROM runner_allocations a
    LEFT JOIN runner_provider_repositories r ON r.provider = a.provider AND r.provider_repository_id = a.provider_repository_id
    WHERE r.org_id::text = $1
    ORDER BY a.created_at,
             a.allocation_id
) t
`

type ExportSandboxRunnerAllocationsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxRunnerAllocationsJSONL(ctx context.Context, arg ExportSandboxRunnerAllocationsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxRunnerAllocationsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxRunnerJobBindingsJSONL = `-- name: ExportSandboxRunnerJobBindingsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT b.binding_id, b.allocation_id, b.provider, b.provider_job_id, b.provider_runner_id, b.runner_name, b.bound_at, b.created_at
    FROM runner_job_bindings b
    JOIN runner_allocations a ON a.allocation_id = b.allocation_id
    LEFT JOIN runner_provider_repositories r ON r.provider = a.provider AND r.provider_repository_id = a.provider_repository_id
    WHERE r.org_id::text = $1
    ORDER BY b.created_at,
             b.binding_id
) t
`

type ExportSandboxRunnerJobBindingsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxRunnerJobBindingsJSONL(ctx context.Context, arg ExportSandboxRunnerJobBindingsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxRunnerJobBindingsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxRunnerJobsJSONL = `-- name: ExportSandboxRunnerJobsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT j.provider, j.provider_job_id, j.provider_installation_id, j.provider_repository_id, j.repository_full_name, j.provider_run_id, j.provider_task_id, j.provider_job_handle, j.job_name, j.head_sha, j.head_branch, j.workflow_name, j.status, j.conclusion, j.labels_json, j.runner_id, j.runner_name, j.started_at, j.completed_at, j.last_webhook_delivery, j.updated_at, j.created_at
    FROM runner_jobs j
    LEFT JOIN runner_provider_repositories r ON r.provider = j.provider AND r.provider_repository_id = j.provider_repository_id
    WHERE r.org_id::text = $1
    ORDER BY j.created_at,
             j.provider,
             j.provider_job_id
) t
`

type ExportSandboxRunnerJobsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxRunnerJobsJSONL(ctx context.Context, arg ExportSandboxRunnerJobsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxRunnerJobsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxRunnerProviderRepositoriesJSONL = `-- name: ExportSandboxRunnerProviderRepositoriesJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (SELECT provider, provider_repository_id, org_id, project_id, source_repository_id, provider_owner, provider_repo, repository_full_name, active, created_at, updated_at FROM runner_provider_repositories WHERE org_id::text = $1 ORDER BY created_at, provider, provider_repository_id) t
`

type ExportSandboxRunnerProviderRepositoriesJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxRunnerProviderRepositoriesJSONL(ctx context.Context, arg ExportSandboxRunnerProviderRepositoriesJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxRunnerProviderRepositoriesJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxRunnerStickyDiskGenerationsJSONL = `-- name: ExportSandboxRunnerStickyDiskGenerationsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (
    SELECT g.provider, g.provider_installation_id, g.provider_repository_id, g.key_hash, g.key, g.current_generation, g.current_source_ref, g.created_at, g.updated_at
    FROM runner_sticky_disk_generations g
    LEFT JOIN runner_provider_repositories r ON r.provider = g.provider AND r.provider_repository_id = g.provider_repository_id
    WHERE r.org_id::text = $1
    ORDER BY g.updated_at,
             g.provider,
             g.provider_installation_id,
             g.provider_repository_id,
             g.key_hash
) t
`

type ExportSandboxRunnerStickyDiskGenerationsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxRunnerStickyDiskGenerationsJSONL(ctx context.Context, arg ExportSandboxRunnerStickyDiskGenerationsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxRunnerStickyDiskGenerationsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportSandboxVMResourceBoundsJSONL = `-- name: ExportSandboxVMResourceBoundsJSONL :many
SELECT row_to_json(t)::text AS row_json
FROM (SELECT org_id, min_vcpus, max_vcpus, min_memory_mib, max_memory_mib, min_root_disk_gib, max_root_disk_gib, created_at, updated_at FROM vm_resource_bounds WHERE org_id::text = $1 ORDER BY updated_at) t
`

type ExportSandboxVMResourceBoundsJSONLParams struct {
	OrgID int64
}

func (q *Queries) ExportSandboxVMResourceBoundsJSONL(ctx context.Context, arg ExportSandboxVMResourceBoundsJSONLParams) ([]string, error) {
	rows, err := q.db.Query(ctx, exportSandboxVMResourceBoundsJSONL, arg.OrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var row_json string
		if err := rows.Scan(&row_json); err != nil {
			return nil, err
		}
		items = append(items, row_json)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
