// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: reads.sql

package store

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getEnvironmentByID = `-- name: GetEnvironmentByID :one
SELECT environment_id, project_id, org_id, slug, display_name, kind, state, protection_policy, version, created_by, updated_by, created_at, updated_at, archived_at
FROM project_environments
WHERE org_id = $1 AND project_id = $2 AND environment_id = $3
`

type GetEnvironmentByIDParams struct {
	OrgID         int64
	ProjectID     uuid.UUID
	EnvironmentID uuid.UUID
}

func (q *Queries) GetEnvironmentByID(ctx context.Context, arg GetEnvironmentByIDParams) (ProjectEnvironment, error) {
	row := q.db.QueryRow(ctx, getEnvironmentByID, arg.OrgID, arg.ProjectID, arg.EnvironmentID)
	var i ProjectEnvironment
	err := row.Scan(
		&i.EnvironmentID,
		&i.ProjectID,
		&i.OrgID,
		&i.Slug,
		&i.DisplayName,
		&i.Kind,
		&i.State,
		&i.ProtectionPolicy,
		&i.Version,
		&i.CreatedBy,
		&i.UpdatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ArchivedAt,
	)
	return i, err
}

const getEnvironmentBySlug = `-- name: GetEnvironmentBySlug :one
SELECT environment_id, project_id, org_id, slug, display_name, kind, state, protection_policy, version, created_by, updated_by, created_at, updated_at, archived_at
FROM project_environments
WHERE org_id = $1 AND project_id = $2 AND slug = $3
`

type GetEnvironmentBySlugParams struct {
	OrgID     int64
	ProjectID uuid.UUID
	Slug      string
}

func (q *Queries) GetEnvironmentBySlug(ctx context.Context, arg GetEnvironmentBySlugParams) (ProjectEnvironment, error) {
	row := q.db.QueryRow(ctx, getEnvironmentBySlug, arg.OrgID, arg.ProjectID, arg.Slug)
	var i ProjectEnvironment
	err := row.Scan(
		&i.EnvironmentID,
		&i.ProjectID,
		&i.OrgID,
		&i.Slug,
		&i.DisplayName,
		&i.Kind,
		&i.State,
		&i.ProtectionPolicy,
		&i.Version,
		&i.CreatedBy,
		&i.UpdatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ArchivedAt,
	)
	return i, err
}

const getProjectByID = `-- name: GetProjectByID :one
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1 AND project_id = $2
`

type GetProjectByIDParams struct {
	OrgID     int64
	ProjectID uuid.UUID
}

func (q *Queries) GetProjectByID(ctx context.Context, arg GetProjectByIDParams) (Project, error) {
	row := q.db.QueryRow(ctx, getProjectByID, arg.OrgID, arg.ProjectID)
	var i Project
	err := row.Scan(
		&i.ProjectID,
		&i.OrgID,
		&i.Slug,
		&i.DisplayName,
		&i.Description,
		&i.State,
		&i.Version,
		&i.CreatedBy,
		&i.UpdatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ArchivedAt,
	)
	return i, err
}

const getProjectByRedirectSlug = `-- name: GetProjectByRedirectSlug :one
SELECT p.project_id, p.org_id, p.slug, p.display_name, p.description, p.state, p.version, p.created_by, p.updated_by, p.created_at, p.updated_at, p.archived_at
FROM project_slug_redirects r
JOIN projects p ON p.project_id = r.project_id AND p.org_id = r.org_id
WHERE r.org_id = $1 AND r.slug = $2
`

type GetProjectByRedirectSlugParams struct {
	OrgID int64
	Slug  string
}

func (q *Queries) GetProjectByRedirectSlug(ctx context.Context, arg GetProjectByRedirectSlugParams) (Project, error) {
	row := q.db.QueryRow(ctx, getProjectByRedirectSlug, arg.OrgID, arg.Slug)
	var i Project
	err := row.Scan(
		&i.ProjectID,
		&i.OrgID,
		&i.Slug,
		&i.DisplayName,
		&i.Description,
		&i.State,
		&i.Version,
		&i.CreatedBy,
		&i.UpdatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ArchivedAt,
	)
	return i, err
}

const getProjectBySlug = `-- name: GetProjectBySlug :one
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1 AND slug = $2
`

type GetProjectBySlugParams struct {
	OrgID int64
	Slug  string
}

func (q *Queries) GetProjectBySlug(ctx context.Context, arg GetProjectBySlugParams) (Project, error) {
	row := q.db.QueryRow(ctx, getProjectBySlug, arg.OrgID, arg.Slug)
	var i Project
	err := row.Scan(
		&i.ProjectID,
		&i.OrgID,
		&i.Slug,
		&i.DisplayName,
		&i.Description,
		&i.State,
		&i.Version,
		&i.CreatedBy,
		&i.UpdatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ArchivedAt,
	)
	return i, err
}

const getProjectIdempotencyRecord = `-- name: GetProjectIdempotencyRecord :one
SELECT result_kind, request_hash, result_payload
FROM project_idempotency_records
WHERE org_id = $1 AND operation = $2 AND key_hash = $3
`

type GetProjectIdempotencyRecordParams struct {
	OrgID     int64
	Operation string
	KeyHash   string
}

type GetProjectIdempotencyRecordRow struct {
	ResultKind    string
	RequestHash   string
	ResultPayload []byte
}

func (q *Queries) GetProjectIdempotencyRecord(ctx context.Context, arg GetProjectIdempotencyRecordParams) (GetProjectIdempotencyRecordRow, error) {
	row := q.db.QueryRow(ctx, getProjectIdempotencyRecord, arg.OrgID, arg.Operation, arg.KeyHash)
	var i GetProjectIdempotencyRecordRow
	err := row.Scan(&i.ResultKind, &i.RequestHash, &i.ResultPayload)
	return i, err
}

const listEnvironments = `-- name: ListEnvironments :many
SELECT environment_id, project_id, org_id, slug, display_name, kind, state, protection_policy, version, created_by, updated_by, created_at, updated_at, archived_at
FROM project_environments
WHERE org_id = $1 AND project_id = $2
ORDER BY kind, slug
`

type ListEnvironmentsParams struct {
	OrgID     int64
	ProjectID uuid.UUID
}

func (q *Queries) ListEnvironments(ctx context.Context, arg ListEnvironmentsParams) ([]ProjectEnvironment, error) {
	rows, err := q.db.Query(ctx, listEnvironments, arg.OrgID, arg.ProjectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProjectEnvironment{}
	for rows.Next() {
		var i ProjectEnvironment
		if err := rows.Scan(
			&i.EnvironmentID,
			&i.ProjectID,
			&i.OrgID,
			&i.Slug,
			&i.DisplayName,
			&i.Kind,
			&i.State,
			&i.ProtectionPolicy,
			&i.Version,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEvents = `-- name: ListEvents :many
SELECT event_id, org_id, project_id, environment_id, event_type, actor_id, payload, trace_id, traceparent, created_at
FROM project_events
WHERE org_id = $1
ORDER BY created_at DESC, event_id DESC
LIMIT $2
`

type ListEventsParams struct {
	OrgID int64
	Limit int32
}

func (q *Queries) ListEvents(ctx context.Context, arg ListEventsParams) ([]ProjectEvent, error) {
	rows, err := q.db.Query(ctx, listEvents, arg.OrgID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProjectEvent{}
	for rows.Next() {
		var i ProjectEvent
		if err := rows.Scan(
			&i.EventID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.EventType,
			&i.ActorID,
			&i.Payload,
			&i.TraceID,
			&i.Traceparent,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEventsAfterCursor = `-- name: ListEventsAfterCursor :many
SELECT event_id, org_id, project_id, environment_id, event_type, actor_id, payload, trace_id, traceparent, created_at
FROM project_events
WHERE org_id = $1 AND (created_at, event_id) < ($2::timestamptz, $3::uuid)
ORDER BY created_at DESC, event_id DESC
LIMIT $4
`

type ListEventsAfterCursorParams struct {
	OrgID         int64
	CreatedBefore pgtype.Timestamptz
	IDBefore      uuid.UUID
	LimitCount    int32
}

func (q *Queries) ListEventsAfterCursor(ctx context.Context, arg ListEventsAfterCursorParams) ([]ProjectEvent, error) {
	rows, err := q.db.Query(ctx, listEventsAfterCursor,
		arg.OrgID,
		arg.CreatedBefore,
		arg.IDBefore,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProjectEvent{}
	for rows.Next() {
		var i ProjectEvent
		if err := rows.Scan(
			&i.EventID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.EventType,
			&i.ActorID,
			&i.Payload,
			&i.TraceID,
			&i.Traceparent,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProjects = `-- name: ListProjects :many
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1
ORDER BY created_at DESC, project_id DESC
LIMIT $2
`

type ListProjectsParams struct {
	OrgID int64
	Limit int32
}

func (q *Queries) ListProjects(ctx context.Context, arg ListProjectsParams) ([]Project, error) {
	rows, err := q.db.Query(ctx, listProjects, arg.OrgID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Project{}
	for rows.Next() {
		var i Project
		if err := rows.Scan(
			&i.ProjectID,
			&i.OrgID,
			&i.Slug,
			&i.DisplayName,
			&i.Description,
			&i.State,
			&i.Version,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProjectsAfterCursor = `-- name: ListProjectsAfterCursor :many
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1 AND (created_at, project_id) < ($2::timestamptz, $3::uuid)
ORDER BY created_at DESC, project_id DESC
LIMIT $4
`

type ListProjectsAfterCursorParams struct {
	OrgID         int64
	CreatedBefore pgtype.Timestamptz
	IDBefore      uuid.UUID
	LimitCount    int32
}

func (q *Queries) ListProjectsAfterCursor(ctx context.Context, arg ListProjectsAfterCursorParams) ([]Project, error) {
	rows, err := q.db.Query(ctx, listProjectsAfterCursor,
		arg.OrgID,
		arg.CreatedBefore,
		arg.IDBefore,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Project{}
	for rows.Next() {
		var i Project
		if err := rows.Scan(
			&i.ProjectID,
			&i.OrgID,
			&i.Slug,
			&i.DisplayName,
			&i.Description,
			&i.State,
			&i.Version,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProjectsByState = `-- name: ListProjectsByState :many
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1 AND state = $2
ORDER BY created_at DESC, project_id DESC
LIMIT $3
`

type ListProjectsByStateParams struct {
	OrgID int64
	State string
	Limit int32
}

func (q *Queries) ListProjectsByState(ctx context.Context, arg ListProjectsByStateParams) ([]Project, error) {
	rows, err := q.db.Query(ctx, listProjectsByState, arg.OrgID, arg.State, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Project{}
	for rows.Next() {
		var i Project
		if err := rows.Scan(
			&i.ProjectID,
			&i.OrgID,
			&i.Slug,
			&i.DisplayName,
			&i.Description,
			&i.State,
			&i.Version,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProjectsByStateAfterCursor = `-- name: ListProjectsByStateAfterCursor :many
SELECT project_id, org_id, slug, display_name, description, state, version, created_by, updated_by, created_at, updated_at, archived_at
FROM projects
WHERE org_id = $1 AND state = $2 AND (created_at, project_id) < ($3::timestamptz, $4::uuid)
ORDER BY created_at DESC, project_id DESC
LIMIT $5
`

type ListProjectsByStateAfterCursorParams struct {
	OrgID         int64
	State         string
	CreatedBefore pgtype.Timestamptz
	IDBefore      uuid.UUID
	LimitCount    int32
}

func (q *Queries) ListProjectsByStateAfterCursor(ctx context.Context, arg ListProjectsByStateAfterCursorParams) ([]Project, error) {
	rows, err := q.db.Query(ctx, listProjectsByStateAfterCursor,
		arg.OrgID,
		arg.State,
		arg.CreatedBefore,
		arg.IDBefore,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Project{}
	for rows.Next() {
		var i Project
		if err := rows.Scan(
			&i.ProjectID,
			&i.OrgID,
			&i.Slug,
			&i.DisplayName,
			&i.Description,
			&i.State,
			&i.Version,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const projectSlugUnavailable = `-- name: ProjectSlugUnavailable :one
SELECT EXISTS (
    SELECT 1 FROM projects p WHERE p.org_id = $1 AND p.slug = $2
) OR EXISTS (
    SELECT 1 FROM project_slug_redirects r WHERE r.org_id = $1 AND r.slug = $2
) AS unavailable
`

type ProjectSlugUnavailableParams struct {
	OrgID int64
	Slug  string
}

func (q *Queries) ProjectSlugUnavailable(ctx context.Context, arg ProjectSlugUnavailableParams) (pgtype.Bool, error) {
	row := q.db.QueryRow(ctx, projectSlugUnavailable, arg.OrgID, arg.Slug)
	var unavailable pgtype.Bool
	err := row.Scan(&unavailable)
	return unavailable, err
}

const projectSlugUnavailableForOtherProject = `-- name: ProjectSlugUnavailableForOtherProject :one
SELECT EXISTS (
    SELECT 1 FROM projects p WHERE p.org_id = $1 AND p.slug = $2 AND p.project_id <> $3
) OR EXISTS (
    SELECT 1 FROM project_slug_redirects r WHERE r.org_id = $1 AND r.slug = $2
) AS unavailable
`

type ProjectSlugUnavailableForOtherProjectParams struct {
	OrgID     int64
	Slug      string
	ProjectID uuid.UUID
}

func (q *Queries) ProjectSlugUnavailableForOtherProject(ctx context.Context, arg ProjectSlugUnavailableForOtherProjectParams) (pgtype.Bool, error) {
	row := q.db.QueryRow(ctx, projectSlugUnavailableForOtherProject, arg.OrgID, arg.Slug, arg.ProjectID)
	var unavailable pgtype.Bool
	err := row.Scan(&unavailable)
	return unavailable, err
}
