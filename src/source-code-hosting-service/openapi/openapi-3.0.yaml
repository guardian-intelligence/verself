components:
  schemas:
    Blob:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/Blob.json
          format: uri
          readOnly: true
          type: string
        content:
          type: string
        download_url:
          type: string
        encoding:
          type: string
        name:
          type: string
        path:
          type: string
        sha:
          type: string
        size:
          format: int64
          maximum: 9007199254740991
          minimum: 0
          type: integer
      required:
        - path
        - name
        - encoding
        - content
        - size
        - sha
      type: object
    CheckoutGrant:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/CheckoutGrant.json
          format: uri
          readOnly: true
          type: string
        expires_at:
          format: date-time
          type: string
        grant_id:
          type: string
        ref:
          type: string
        repo_id:
          type: string
        token:
          type: string
      required:
        - grant_id
        - repo_id
        - ref
        - token
        - expires_at
      type: object
    CreateCheckoutGrantRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/CreateCheckoutGrantRequest.json
          format: uri
          readOnly: true
          type: string
        path_prefix:
          maxLength: 1024
          type: string
        ref:
          maxLength: 255
          type: string
      type: object
    CreateIntegrationRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/CreateIntegrationRequest.json
          format: uri
          readOnly: true
          type: string
        credential_ref:
          maxLength: 512
          type: string
        external_repo:
          maxLength: 512
          minLength: 1
          type: string
        provider:
          maxLength: 64
          minLength: 1
          type: string
      required:
        - provider
        - external_repo
      type: object
    CreateRepositoryRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/CreateRepositoryRequest.json
          format: uri
          readOnly: true
          type: string
        default_branch:
          maxLength: 128
          type: string
        description:
          maxLength: 1024
          type: string
        name:
          maxLength: 128
          minLength: 1
          type: string
      required:
        - name
      type: object
    CreateWorkflowRunRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/CreateWorkflowRunRequest.json
          format: uri
          readOnly: true
          type: string
        inputs:
          additionalProperties:
            type: string
          type: object
        ref:
          maxLength: 255
          type: string
        workflow_path:
          maxLength: 512
          minLength: 1
          type: string
      required:
        - workflow_path
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          example: Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          nullable: true
          type: array
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          example: https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          example: 400
          format: int64
          maximum: 599
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          example: Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          example: https://example.com/errors/example
          format: uri
          type: string
      type: object
    ExternalIntegration:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/ExternalIntegration.json
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        credential_ref:
          type: string
        external_repo:
          type: string
        integration_id:
          type: string
        org_id:
          type: string
        provider:
          type: string
        state:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - integration_id
        - org_id
        - provider
        - external_repo
        - state
        - created_at
        - updated_at
      type: object
    Ref:
      additionalProperties: false
      properties:
        commit:
          type: string
        name:
          type: string
      required:
        - name
        - commit
      type: object
    RefList:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/RefList.json
          format: uri
          readOnly: true
          type: string
        refs:
          items:
            $ref: "#/components/schemas/Ref"
          nullable: true
          type: array
      required:
        - refs
      type: object
    Repository:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/Repository.json
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        default_branch:
          type: string
        description:
          type: string
        name:
          type: string
        org_id:
          type: string
        provider:
          type: string
        repo_id:
          type: string
        slug:
          type: string
        state:
          type: string
        updated_at:
          format: date-time
          type: string
        version:
          format: int32
          maximum: 2147483647
          minimum: 0
          type: integer
        visibility:
          type: string
      required:
        - repo_id
        - org_id
        - name
        - slug
        - description
        - default_branch
        - provider
        - visibility
        - state
        - version
        - created_at
        - updated_at
      type: object
    RepositoryList:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/RepositoryList.json
          format: uri
          readOnly: true
          type: string
        repositories:
          items:
            $ref: "#/components/schemas/Repository"
          nullable: true
          type: array
      required:
        - repositories
      type: object
    Tree:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/Tree.json
          format: uri
          readOnly: true
          type: string
        entries:
          items:
            $ref: "#/components/schemas/TreeEntry"
          nullable: true
          type: array
      required:
        - entries
      type: object
    TreeEntry:
      additionalProperties: false
      properties:
        path:
          type: string
        sha:
          type: string
        size:
          format: int64
          maximum: 9007199254740991
          minimum: 0
          type: integer
        type:
          type: string
      required:
        - path
        - type
        - size
        - sha
      type: object
    WorkflowRun:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/WorkflowRun.json
          format: uri
          readOnly: true
          type: string
        actor_id:
          type: string
        created_at:
          format: date-time
          type: string
        dispatched_at:
          format: date-time
          type: string
        failure_reason:
          type: string
        inputs:
          additionalProperties:
            type: string
          type: object
        org_id:
          type: string
        provider:
          type: string
        provider_dispatch_id:
          type: string
        ref:
          type: string
        repo_id:
          type: string
        state:
          type: string
        trace_id:
          type: string
        updated_at:
          format: date-time
          type: string
        workflow_path:
          type: string
        workflow_run_id:
          type: string
      required:
        - workflow_run_id
        - org_id
        - repo_id
        - actor_id
        - provider
        - workflow_path
        - ref
        - inputs
        - state
        - created_at
        - updated_at
      type: object
    WorkflowRunList:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          example: https://source.api.anveio.com/schemas/WorkflowRunList.json
          format: uri
          readOnly: true
          type: string
        workflow_runs:
          items:
            $ref: "#/components/schemas/WorkflowRun"
          nullable: true
          type: array
      required:
        - workflow_runs
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: Zitadel OIDC access token for a human subject.
      scheme: bearer
      type: http
    mutualTLS:
      description: SPIFFE mTLS between Forge Metal workloads.
      type: mutualTLS
info:
  title: Source Code Hosting Service
  version: 1.0.0
openapi: 3.0.3
paths:
  /api/v1/integrations:
    post:
      operationId: create-source-integration
      parameters:
        - description: Stable caller-provided key used to make this mutation retry-safe.
          in: header
          name: Idempotency-Key
          required: true
          schema:
            maxLength: 128
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateIntegrationRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExternalIntegration"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Register an external source integration
      x-forge-metal-iam:
        action: create
        audit_event: source.integration.create
        data_classification: customer_source_metadata
        event_category: source
        operation_display: create source integration
        operation_type: write
        org_scope: token_org_id
        permission: source:integration:write
        rate_limit_class: source_mutation
        resource: source_integration
        risk_level: high
        source_product_area: Source
  /api/v1/repos:
    get:
      operationId: list-source-repositories
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RepositoryList"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: List source repositories
      x-forge-metal-iam:
        action: list
        audit_event: source.repo.list
        data_classification: customer_source_metadata
        event_category: source
        operation_display: list source repositories
        operation_type: read
        org_scope: token_org_id
        permission: source:repo:read
        rate_limit_class: read
        resource: source_repository
        risk_level: low
        source_product_area: Source
    post:
      operationId: create-source-repository
      parameters:
        - description: Stable caller-provided key used to make this mutation retry-safe.
          in: header
          name: Idempotency-Key
          required: true
          schema:
            maxLength: 128
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRepositoryRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Repository"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Create a source repository
      x-forge-metal-iam:
        action: create
        audit_event: source.repo.create
        data_classification: customer_source_metadata
        event_category: source
        operation_display: create source repository
        operation_type: write
        org_scope: token_org_id
        permission: source:repo:write
        rate_limit_class: source_mutation
        resource: source_repository
        risk_level: medium
        source_product_area: Source
  /api/v1/repos/{repo_id}:
    get:
      operationId: get-source-repository
      parameters:
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Repository"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Get a source repository
      x-forge-metal-iam:
        action: read
        audit_event: source.repo.read
        data_classification: customer_source_metadata
        event_category: source
        operation_display: read source repository
        operation_type: read
        org_scope: token_org_id
        permission: source:repo:read
        rate_limit_class: read
        resource: source_repository
        risk_level: low
        source_product_area: Source
  /api/v1/repos/{repo_id}/blob:
    get:
      operationId: get-source-blob
      parameters:
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
        - explode: false
          in: query
          name: ref
          schema:
            maxLength: 255
            type: string
        - explode: false
          in: query
          name: path
          required: true
          schema:
            maxLength: 2048
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Blob"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Get a repository blob
      x-forge-metal-iam:
        action: read
        audit_event: source.blob.get
        data_classification: customer_source_metadata
        event_category: source
        operation_display: read source blob
        operation_type: read
        org_scope: token_org_id
        permission: source:repo:read
        rate_limit_class: read
        resource: source_blob
        risk_level: medium
        source_product_area: Source
  /api/v1/repos/{repo_id}/checkout-grants:
    post:
      operationId: create-source-checkout-grant
      parameters:
        - description: Stable caller-provided key used to make this mutation retry-safe.
          in: header
          name: Idempotency-Key
          required: true
          schema:
            maxLength: 128
            minLength: 1
            type: string
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCheckoutGrantRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutGrant"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Create a short-lived checkout grant
      x-forge-metal-iam:
        action: create
        audit_event: source.checkout_grant.create
        data_classification: customer_source_metadata
        event_category: source
        operation_display: create source checkout grant
        operation_type: write
        org_scope: token_org_id
        permission: source:checkout:write
        rate_limit_class: source_mutation
        resource: source_checkout_grant
        risk_level: high
        source_product_area: Source
  /api/v1/repos/{repo_id}/refs:
    get:
      operationId: list-source-refs
      parameters:
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefList"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: List repository refs
      x-forge-metal-iam:
        action: list
        audit_event: source.refs.list
        data_classification: customer_source_metadata
        event_category: source
        operation_display: list source refs
        operation_type: read
        org_scope: token_org_id
        permission: source:repo:read
        rate_limit_class: read
        resource: source_ref
        risk_level: low
        source_product_area: Source
  /api/v1/repos/{repo_id}/tree:
    get:
      operationId: get-source-tree
      parameters:
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
        - explode: false
          in: query
          name: ref
          schema:
            maxLength: 255
            type: string
        - explode: false
          in: query
          name: path
          schema:
            maxLength: 2048
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tree"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Get a repository tree path
      x-forge-metal-iam:
        action: read
        audit_event: source.tree.get
        data_classification: customer_source_metadata
        event_category: source
        operation_display: read source tree
        operation_type: read
        org_scope: token_org_id
        permission: source:repo:read
        rate_limit_class: read
        resource: source_tree
        risk_level: low
        source_product_area: Source
  /api/v1/repos/{repo_id}/workflow-runs:
    get:
      operationId: list-source-workflow-runs
      parameters:
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowRunList"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: List repository workflow runs
      x-forge-metal-iam:
        action: list
        audit_event: source.workflow_run.list
        data_classification: customer_source_metadata
        event_category: source
        operation_display: list source workflow runs
        operation_type: read
        org_scope: token_org_id
        permission: source:workflow:read
        rate_limit_class: read
        resource: source_workflow_run
        risk_level: low
        source_product_area: Source
    post:
      operationId: create-source-workflow-run
      parameters:
        - description: Stable caller-provided key used to make this mutation retry-safe.
          in: header
          name: Idempotency-Key
          required: true
          schema:
            maxLength: 128
            minLength: 1
            type: string
        - in: path
          name: repo_id
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkflowRunRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowRun"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Dispatch a repository workflow
      x-forge-metal-iam:
        action: create
        audit_event: source.workflow.dispatch
        data_classification: customer_source_metadata
        event_category: source
        operation_display: dispatch source workflow
        operation_type: write
        org_scope: token_org_id
        permission: source:workflow:write
        rate_limit_class: source_mutation
        resource: source_workflow_run
        risk_level: high
        source_product_area: Source
  /api/v1/workflow-runs/{workflow_run_id}:
    get:
      operationId: get-source-workflow-run
      parameters:
        - in: path
          name: workflow_run_id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowRun"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Get a workflow run
      x-forge-metal-iam:
        action: read
        audit_event: source.workflow_run.read
        data_classification: customer_source_metadata
        event_category: source
        operation_display: read source workflow run
        operation_type: read
        org_scope: token_org_id
        permission: source:workflow:read
        rate_limit_class: read
        resource: source_workflow_run
        risk_level: low
        source_product_area: Source
servers:
  - url: https://source.api.anveio.com
