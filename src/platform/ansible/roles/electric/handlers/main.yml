---
# Handler name resolves at handler-load time, so each include_role invocation
# produces a distinct handler. Tasks notify the concrete service name.
- name: "Restart {{ electric_service_name }}"
  ansible.builtin.systemd:
    name: "{{ electric_service_name }}"
    state: restarted
    daemon_reload: true
