---
# Ensure verself_bin is in the system PATH for all users.

- name: Add profile bin to system PATH
  ansible.builtin.copy:
    content: |
      export PATH="{{ verself_bin }}:$PATH"
    dest: /etc/profile.d/verself.sh
    mode: "0644"
