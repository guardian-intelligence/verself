---
argument_specs:
  main:
    short_description: "Host firewall via nftables (default-deny ingress)"
    description:
      - "Copies cue-renderer generated final nftables files."
      - "The CUE ruleset records are the topology contract; Ansible only installs them."
    options: {}
