#!/usr/sbin/nft -f
# Load all rulesets from /etc/nftables.d/.
# Managed by Ansible — do not edit manually.
#
# No "flush ruleset" — each .nft file does its own atomic table replace,
# and provider rules (e.g. Latitude.sh metadata DNAT) are left untouched.
include "/etc/nftables.d/*.nft"
