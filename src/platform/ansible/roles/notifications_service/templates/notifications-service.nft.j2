# Notifications service is loopback-only. It needs PostgreSQL, ClickHouse,
# NATS JetStream, Zitadel OIDC discovery via local Caddy, and OTLP egress.

table inet verself_notifications_service
delete table inet verself_notifications_service

table inet verself_notifications_service {
    chain input {
        type filter hook input priority filter; policy accept;

        tcp dport {{ topology_endpoints.notifications_service.endpoints.public_http.port }} iifname != "lo" drop
    }

    chain output {
        type filter hook output priority filter; policy accept;

        ct state established,related accept
        meta skuid != notifications_service accept

        oifname "lo" tcp dport {{ topology_endpoints.postgresql.endpoints.postgres.port }} accept
        oifname "lo" tcp dport {{ topology_endpoints.clickhouse.endpoints.native_tls.port }} accept
        oifname "lo" tcp dport {{ topology_endpoints.nats.endpoints.client.port }} accept
        oifname "lo" tcp dport {{ topology_endpoints.zitadel.endpoints.http.port }} accept
        oifname "lo" tcp dport 443 accept
        oifname "lo" tcp dport {{ topology_endpoints.otelcol.endpoints.otlp_grpc.port }} accept
        meta l4proto != tcp meta l4proto != udp accept

        drop
    }
}
