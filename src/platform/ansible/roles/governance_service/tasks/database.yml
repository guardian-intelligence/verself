---
- name: Ensure governance_service PostgreSQL role and database exist
  module_defaults:
    community.postgresql.postgresql_user:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      trust_input: false
    community.postgresql.postgresql_db:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      maintenance_db: postgres
      trust_input: false
    community.postgresql.postgresql_owner:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      login_db: postgres
      trust_input: false
  block:
    - name: Ensure governance_service PostgreSQL user exists
      community.postgresql.postgresql_user:
        name: governance_service
        password: "{{ governance_service_pg_password }}"
        role_attr_flags: LOGIN
        conn_limit: "{{ postgresql_role_connection_limits.governance_service }}"
      no_log: true

    - name: Ensure governance_service database exists
      community.postgresql.postgresql_db:
        name: governance_service
        owner: governance_service
        state: present
      no_log: true

    - name: Ensure governance_service owns governance_service database
      community.postgresql.postgresql_owner:
        obj_name: governance_service
        obj_type: database
        new_owner: governance_service
      no_log: true
