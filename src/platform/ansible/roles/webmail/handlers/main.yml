---
- name: Restart webmail
  ansible.builtin.systemd:
    name: webmail
    state: restarted
    daemon_reload: true

- name: Reload nftables
  ansible.builtin.systemd:
    name: nftables
    state: reloaded
