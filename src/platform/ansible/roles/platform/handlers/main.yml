---
- name: Restart platform
  ansible.builtin.systemd:
    name: platform
    state: restarted
    daemon_reload: true

- name: Reload nftables
  ansible.builtin.systemd:
    name: nftables
    state: reloaded
