# Platform docs frontend — loopback only, Caddy reverse proxy.
table inet forge_metal_platform
delete table inet forge_metal_platform
table inet forge_metal_platform {
    chain input {
        type filter hook input priority filter; policy accept;
        tcp dport {{ services.platform.port }} iifname != "lo" drop
    }
}
