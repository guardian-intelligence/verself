# Platform docs frontend — loopback only, Caddy reverse proxy.
table inet verself_platform
delete table inet verself_platform
table inet verself_platform {
    chain input {
        type filter hook input priority filter; policy accept;
        tcp dport {{ topology_endpoints.platform.endpoints.http.port }} iifname != "lo" drop
    }
}
