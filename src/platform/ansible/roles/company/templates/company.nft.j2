# Guardian Intelligence company site — loopback only, Caddy reverse proxy.
table inet forge_metal_company
delete table inet forge_metal_company
table inet forge_metal_company {
    chain input {
        type filter hook input priority filter; policy accept;
        tcp dport {{ services.company.port }} iifname != "lo" drop
    }
}
