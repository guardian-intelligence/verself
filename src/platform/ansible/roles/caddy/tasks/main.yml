---
# Caddy reverse proxy with automatic TLS.
# Binary provided by substrate_profile role via server_tools.tar.zst.

- name: Create Caddy system group
  ansible.builtin.group:
    name: caddy
    system: true

- name: Create Caddy system user
  ansible.builtin.user:
    name: caddy
    group: caddy
    groups: adm
    append: true
    system: true
    home: /var/lib/caddy
    shell: /usr/sbin/nologin
    create_home: false

- name: Create Caddy config directory
  ansible.builtin.file:
    path: /etc/caddy
    state: directory
    owner: root
    group: caddy
    mode: "0750"

- name: Create Caddy state directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: caddy
    group: caddy
    mode: "0750"
  loop:
    - /var/lib/caddy
    - /var/cache/caddy
    - "{{ nomad_artifacts_dir }}"

- name: Create Caddy log directory
  ansible.builtin.file:
    path: /var/log/caddy
    state: directory
    owner: caddy
    group: adm
    mode: "2750"

- name: Create Caddy access log with otelcol-readable permissions
  ansible.builtin.file:
    path: /var/log/caddy/access.log
    state: touch
    owner: caddy
    group: adm
    mode: "0640"

- name: Read Electric API secret for shape proxy auth
  ansible.builtin.slurp:
    src: /etc/credstore/electric/api-secret
  register: caddy_electric_secret_raw
  no_log: true

- name: Set Electric secret fact
  ansible.builtin.set_fact:
    caddy_electric_api_secret: "{{ caddy_electric_secret_raw.content | b64decode | trim }}"
  no_log: true

- name: Read Electric Mail API secret for shape proxy auth
  ansible.builtin.slurp:
    src: /etc/credstore/electric-mail/api-secret
  register: caddy_electric_mail_secret_raw
  no_log: true

- name: Set Electric Mail secret fact
  ansible.builtin.set_fact:
    caddy_electric_mail_api_secret: "{{ caddy_electric_mail_secret_raw.content | b64decode | trim }}"
  no_log: true

- name: Read Electric Notifications API secret for shape proxy auth
  ansible.builtin.slurp:
    src: /etc/credstore/electric-notifications/api-secret
  register: caddy_electric_notifications_secret_raw
  no_log: true

- name: Set Electric Notifications secret fact
  ansible.builtin.set_fact:
    caddy_electric_notifications_api_secret: "{{ caddy_electric_notifications_secret_raw.content | b64decode | trim }}"
  no_log: true

- name: Require billing webhook DNS to remain DNS-only
  ansible.builtin.assert:
    that: not (cloudflare_proxied | default(false) | bool)
    fail_msg: >-
      billing webhook origin IP allowlisting requires billing DNS to remain
      DNS-only (cloudflare_proxied=false). Move the allowlist to the
      Cloudflare edge before enabling orange-cloud proxying.

- name: Assert Stripe webhook IP list parsed
  ansible.builtin.assert:
    that:
      - caddy_stripe_webhook_ips | length > 0
      - caddy_stripe_webhook_ips | select('string') | list | length == caddy_stripe_webhook_ips | length
    fail_msg: >-
      caddy_stripe_webhook_ips must contain the published Stripe webhook source IPs.

- name: Deploy Caddyfile
  ansible.builtin.template:
    src: Caddyfile.j2
    dest: /etc/caddy/Caddyfile
    owner: root
    group: caddy
    mode: "0640"
  no_log: true
  notify: restart caddy

- name: Deploy Caddy systemd unit
  ansible.builtin.copy:
    content: |
      [Unit]
      Description=Caddy HTTP/2 web server
      After=verself-firewall.target network-online.target verself-firecracker-network.service
      Requires=verself-firewall.target
      Wants=network-online.target verself-firecracker-network.service

      [Service]
      Type=simple
      User=caddy
      Group=caddy
      SupplementaryGroups=adm
      Environment=HOME=/var/lib/caddy
      Environment=XDG_CONFIG_HOME=/etc/caddy
      Environment=XDG_DATA_HOME=/var/lib/caddy
      Environment=XDG_CACHE_HOME=/var/cache/caddy
      ExecStart={{ verself_bin }}/caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
      ExecReload={{ verself_bin }}/caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile
      Restart=on-failure
      RestartSec=5
      AmbientCapabilities=CAP_NET_BIND_SERVICE
      CapabilityBoundingSet=CAP_NET_BIND_SERVICE
      LimitNOFILE=1048576
      NoNewPrivileges=true
      ProtectSystem=strict
      ProtectHome=true
      PrivateTmp=true
      ProtectKernelTunables=true
      ProtectKernelModules=true
      ProtectKernelLogs=true
      ProtectControlGroups=true
      RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX
      LockPersonality=true
      RestrictSUIDSGID=true
      SystemCallArchitectures=native
      ReadWritePaths=/var/lib/caddy /var/cache/caddy /var/log/caddy

      [Install]
      WantedBy=multi-user.target
    dest: /etc/systemd/system/caddy.service
    mode: "0644"
  notify:
    - restart caddy

- name: Enable and start Caddy
  ansible.builtin.systemd:
    name: caddy
    enabled: true
    state: started
    daemon_reload: true
