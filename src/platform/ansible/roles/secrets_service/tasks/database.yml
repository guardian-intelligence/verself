---
- name: Ensure secrets_service PostgreSQL role and database exist
  module_defaults:
    community.postgresql.postgresql_user:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      trust_input: false
    community.postgresql.postgresql_db:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      maintenance_db: postgres
      trust_input: false
    community.postgresql.postgresql_owner:
      login_host: "{{ services.postgresql.host }}"
      login_port: "{{ services.postgresql.port }}"
      login_user: postgres
      login_password: "{{ postgresql_admin_password }}"
      login_db: postgres
      trust_input: false
  block:
    - name: Ensure secrets_service PostgreSQL user exists
      community.postgresql.postgresql_user:
        name: secrets_service
        password: "{{ secrets_service_pg_password }}"
        role_attr_flags: LOGIN
        conn_limit: "{{ postgresql_role_connection_limits.secrets_service }}"
      no_log: true

    - name: Ensure secrets_service database exists
      community.postgresql.postgresql_db:
        name: secrets_service
        owner: secrets_service
        state: present
      no_log: true

    - name: Ensure secrets_service owns secrets_service database
      community.postgresql.postgresql_owner:
        obj_name: secrets_service
        obj_type: database
        new_owner: secrets_service
      no_log: true
