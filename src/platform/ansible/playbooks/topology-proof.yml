---
- name: Topology proof
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Validate generated topology registry and emit proof spans
      ansible.builtin.command:
        cmd: "{{ playbook_dir }}/../../scripts/topology.py check"
      changed_when: false
