[package]
name = "make-skill-exec"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Headless command behavior for make-skill."
license.workspace = true

[lib]
name = "make_skill_exec"
path = "src/lib.rs"

[dependencies]
make-skill-core = { path = "../core" }

[lints]
workspace = true
