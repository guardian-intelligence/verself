minimum_pre_commit_version: "4.5.1"
repos:
  - repo: local
    hooks:
      - id: ansible-lint
        name: ansible-lint
        entry: bash -lc 'cd src/platform/ansible && ansible-lint playbooks roles'
        language: system
        files: ^src/platform/ansible/
        pass_filenames: false
