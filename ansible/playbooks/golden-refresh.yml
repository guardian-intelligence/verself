---
# Rebuild golden images across the fleet.
# Usage: ansible-playbook -i inventory/hosts.ini playbooks/golden-refresh.yml

- name: Refresh golden images on all workers
  hosts: workers
  become: true
  serial: "25%"  # Rolling update, 25% at a time
  roles:
    - golden_image
