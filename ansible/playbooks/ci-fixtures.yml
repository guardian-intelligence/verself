---
# Deploy the CI stack needed for fixture suites on the existing host and run
# the selected suites.

- name: Deploy forge-metal and run CI fixture suites
  hosts: all
  become: true
  roles:
    - nix_deploy
    - base
    - verdaccio
    - forgejo
    - firecracker
    - ci_fixtures
