---
# Run the selected CI fixture suites against the existing deployed host state.

- name: Run selected CI fixture suites
  hosts: all
  become: true
  gather_facts: false
  roles:
    - ci_fixtures
