# Example secrets structure for forge-metal.
# To create your encrypted secrets file, run:
#
#   make setup-sops
#
# To edit secrets later:
#
#   make edit-secrets

# ClickStack admin user (pre-baked on provision)
clickstack_admin_email: "admin@forge-metal.local"
clickstack_admin_password: "replace-me-with-a-real-password"

# Uncomment if using authenticated S3 (not needed with local SeaweedFS)
# litestream_access_key_id: ""
# litestream_secret_access_key: ""
