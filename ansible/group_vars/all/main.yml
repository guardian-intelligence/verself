---
# Global variables for all nodes.

forge_metal_version: "0.1.0"

# Nix server profile — all binaries live here after nix_deploy role.
# Every systemd unit and config references this path.
fm_bin: /opt/forge-metal/profile/bin

# WireGuard
wireguard_interface: wg0
wireguard_port: 51820
wireguard_network: "10.0.0.0/16"
