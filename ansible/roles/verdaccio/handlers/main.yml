---
- name: Reload systemd
  listen: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Verdaccio
  listen: restart verdaccio
  ansible.builtin.systemd:
    daemon_reload: true
    name: verdaccio
    state: restarted
