---
- name: Reload systemd
  listen: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart OTel Collector
  listen: restart otelcol
  ansible.builtin.systemd:
    name: otelcol
    state: restarted
